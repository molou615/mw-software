// ============================================================
// FRONTEND CONFIGURATION
// Edit the values below for your environment.
// The rest of the code imports from here automatically.
// ============================================================

const config = {
  // Demo mode – set to false when connecting to the real backend
  demo: true,

  // API base URL – empty string means same origin (proxy in dev)
  // For production, set to your backend URL, e.g. 'https://api.yourdomain.com'
  apiBaseUrl: '',

  // Application name (shown in the sidebar header)
  appName: 'DocManager',
};

export default config;
